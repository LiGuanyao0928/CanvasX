# 三端功能对齐清单

这个项目在三个平台上跑：macOS 原生客户端（`native_app/`，Swift + AppKit）、
Windows 原生客户端（`windows_app/`，C# + WPF）、Linux 网页版（Python 后端 +
浏览器打开生成的网页，没有独立窗口客户端）。

**后端逻辑（`canvas_*.py` 这些）三端自动共享，不需要在这里追踪**——同步、
成绩计算、日期猜测、资料管理、推送通知都是同一份 Python 代码，改一处三端
一起生效。这份清单只追踪三个客户端"外壳"各自重复实现的界面功能，因为这部分
没有任何机制会自动保持一致，每次改动都要人工分别移植。

改完一个界面功能后，**顺手更新这份表格**，避免像"隐私勾选框"和"记住我"
这两个功能那样，Windows 版一直不知不觉地落后了好几轮对话才被发现。

## 架构变化：设置向导 / 设置面板 / 提醒时间现在是共享网页

这三块以前是 Mac/Windows 各写一份原生 UI（Swift+AppKit / C#+WPF），跟下面
"已知会长期存在的差异"里说的"两边都做真正原生 App 的代价"完全对上——每次改
一个字段、加一个选项，都要在两边分别改一遍，还很容易漏改或者悄悄跑偏
（"隐私勾选框"和"记住我"就是这么漏掉的）。

现在这三块换成了**跟作业看板/课程资料/成绩同样的模式**：普通的 HTML/CSS/JS
页面（`setup_wizard.html` / `settings.html` / `schedule.html`，加上共享的
`app_theme.css` 和 `bridge.js`），放在项目根目录，Mac 和 Windows 各自的
WebView（WKWebView / WebView2）里加载同一份文件，通过一层小小的 JSON 消息桥接
（Mac: `native_app/Bridge.swift`；Windows: `windows_app/Services/NativeBridge.cs`）
调用原生能力（读写配置文件、跑 Python 脚本、开关窗口）。两边原生代码现在只剩
窗口管理、托管一个 WebView、外加这层通用的桥接分发——界面本身的样式/文案/交互
逻辑只有一份，不会再出现"两边各自实现、慢慢跑偏"的问题。下表里这三块的行
标成"✅ 共享网页"，不再分 macOS/Windows 两列打勾。

| 功能 | macOS | Windows | Linux（网页版） |
|---|---|---|---|
| 4 个板块导航（作业看板/资料/成绩/提醒时间） | ✅ | ✅ | ➖ 网页版没有独立导航，网页本身就是内容 |
| 设置向导：连接账号 + 选课程 + 推送方式 | ✅ 共享网页 | ✅ 共享网页 | ➖ 无图形向导，手动编辑 `.env`/`tracked_courses.json` |
| 登录页：数据使用说明勾选框 | ✅ 共享网页 | ✅ 共享网页 | ➖ |
| 登录页："记住我"，不勾选则退出时清本机数据 | ✅ 共享网页 | ✅ 共享网页 | ➖ 网页版本来就不常驻运行，无"退出"概念 |
| 登录页：Token 生成不了的帮助提示 | ✅ 共享网页 | ✅ 共享网页 | ✅（README FAQ） |
| 设置面板：外观（浅色/深色/跟随系统） | ✅ 共享网页 | ✅ 共享网页 | ➖ |
| 设置面板：语言（仅设置面板文字，中/英） | ✅ 共享网页 | ✅ 共享网页 | ➖ |
| 设置面板：账号信息 + 重新选课/换Token | ✅ 共享网页 | ✅ 共享网页 | ➖ 手动改 `.env` |
| 设置面板：退出登录 | ✅ 共享网页 | ✅ 共享网页 | ➖ 手动删文件 |
| 设置面板：立即同步 / 打开日志文件夹 / 关于 | ✅ 共享网页 | ✅ 共享网页 | ➖ |
| 提醒时间：增删改查 + 按星期重复 | ✅ 共享网页 | ✅ 共享网页 | ✅（`schedule.json` + cron，无图形界面） |
| 提醒时间：保存后驱动系统级定时任务 | ✅ launchd | ✅ schtasks | ✅ crontab |
| 课程资料：添加/删除本地文件 | ✅ | ✅（未实机验证） | ➖ 需手动跑 `canvas_materials.py` |
| 日历同步 | ✅ 系统日历 App | ✅ `.ics` 文件 | ✅ `.ics` 文件 |
| 侧边栏可见的设置入口 | ✅（第 5 个导航项） | ✅（独立按钮，非导航列表项，效果等价） | ➖ |

"提醒时间：保存后驱动系统级定时任务"这一行没有跟着变成"共享网页"——
`schedule.html` 这个界面本身共享了，但保存之后"用什么机制真正驱动定时提醒"
（launchd / schtasks / crontab）天然是操作系统级别的东西，没法跨平台共享，
继续各自调用各自的 `update_schedule*.py` 脚本，这是合理的、会长期存在的差异，
不是漏做。

## 已知会长期存在、不需要对齐的差异

- **苹果快捷指令推送**：Mac 专属（Windows/Linux 没有对应物），Windows/Linux
  端的推送方式向导里本来就只有 none/ntfy/discord 三个选项，不是漏做。
- **窗口管理、WebView 托管、桥接分发本身还是两边各写一份原生代码**
  （Swift+AppKit / C#+WPF）——共享的是网页内容，不是整个客户端；这一层薄薄的
  原生外壳仍然要跟着 WebView2/WKWebView 各自的 API 分别维护，但改动量比以前
  小得多，而且不再涉及界面逻辑本身。
- **定时任务的操作系统层实现**（launchd/schtasks/crontab）：见上表说明，
  网页界面共享了，但驱动机制天然是平台专属的。

## 怎么降低"忘记同步"的风险

1. **能下沉到 Python 共享层的逻辑，别放在 Swift/C# 里各写一份。** 比如
   `clear_local_user_data.py` 这份"退出登录要删哪些文件"的清单，Mac 和
   Windows 现在都调用它，而不是各自在 Swift/C# 里硬编码一份文件名列表——
   以后加新的 per-user 生成文件，只用改这一处，两边行为自动一起更新。
   加新的原生功能时，先想一下"这段逻辑是不是其实可以是个 Python 脚本，
   两边客户端都去调用它"，而不是默认写成平台原生代码。
2. **改完一个界面功能，直接问一遍"另一个平台要不要也做"**，而不是攒到
   后面才发现漏了。这份清单就是承接这一步的地方。
3. **两套客户端没有编译期或运行时的检查能防止这种漏做**——这是纯人工流程，
   没有"自动报错"这一层，全靠改动时主动想起来去查这份清单。
