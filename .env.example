# 这个文件仅供参考——正常情况下你不需要手动编辑它。
# 第一次打开 App 时会有设置向导引导你填这些信息，自动生成真正的 .env。
# 只有想跳过向导手动配置时才需要照着这个格式自己写一份 .env。

CANVAS_API_TOKEN=你的Canvas访问令牌（Canvas网页里"账户→设置→新建访问令牌"生成）
CANVAS_BASE_URL=https://yourschool.instructure.com

# 手机推送方式，选一个：none / ntfy / discord / shortcuts
NOTIFY_METHOD=none

# NOTIFY_METHOD=ntfy 时需要：
# NTFY_TOPIC=起一个不容易被猜到的频道名

# NOTIFY_METHOD=discord 时需要（Discord 频道设置→整合→Webhook→新增 Webhook）：
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# NOTIFY_METHOD=shortcuts 时需要（仅 Mac，Windows/Linux 用不了这个选项）：
# 在 Mac"快捷指令"App里建一个接收文本、发消息给自己的快捷指令
# SHORTCUTS_NAME=快捷指令的名字
